name: Deploy when pushed to main

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: fifsky/ssh-action@master
        with:
          command: |
            echo "Hello world"
            exit 1
          host: 164.92.246.13
          user: root
          pass: imF(_2AcG#*fbBn